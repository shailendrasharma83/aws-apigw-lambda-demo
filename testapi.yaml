version: 0.2

phases:
  install:
    runtime-versions: 
      nodejs: 12
    commands:
    - npm install
  build:
    commands:    
      - echo "Run API Tests"
      - echo "Send Email"
artifacts:
  type: html
  files:
    - api-report.html
